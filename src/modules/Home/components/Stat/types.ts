export type StatProps = {
    label: string;
    value: string;
    className?: string;
    // Printed after the value in parentheses — "too little data" beside a thin market's ROI.
    hint?: string;
};
